# API Gaps: metal-ast.json vs. metal-cpp Comparison

This document records API differences between the refactored generator output (from `metal-ast.json`) and the previous output (from `metal-cpp` headers + `metal-docs.json`). Generated by comparing the `master` branch against the `copilot/refactor-metal-net-generator` branch.

---

## Summary

| Category | Count | Status |
|---|---|---|
| Missing types | 1 | Cosmetic — moved into `MTLEnums.cs` |
| Extra types | 1 | New API from SDK |
| Missing base-class protocols | 0 | ~~Fixed~~ — empty marker protocols now included in `metal-ast.json` |
| Missing properties (deprecated `Bool8` aliases) | 23 | Intentional — see below |
| Missing properties (absent from AST) | 4 | Data gap in `metal-ast.json` |
| Missing selectors / methods | 0 | — |
| Method renames | 0 | — |

---

## 1. Missing Type: `MTLIOCompressionStatus`

The `MTLIOCompressionStatus` enum was previously in its own file `Metal/MTLIOCompressionStatus.cs`. It is now emitted inside `Metal/MTLEnums.cs` alongside all other enums. **No API loss.**

## 2. Extra Type: `MTLTilePipelineColorAttachmentDescriptor`

This class was not present in the old `metal-cpp` headers but exists in the SDK's ObjC headers (via `metal-ast.json`). **New API coverage.**

## 3. ~~Missing Base-Class Protocols~~ (RESOLVED)

The following empty marker protocols were absent from `metal-ast.json` but are now included after relaxing the protocol extraction condition in `extract-metal-api.yml`:

| Protocol | Affected Classes | Status |
|---|---|---|
| `MTLFunctionStitchingNode` | `MTLFunctionStitchingInputNode`, `MTLFunctionStitchingFunctionNode` | ✅ Fixed |
| `MTLFunctionStitchingAttribute` | `MTLFunctionStitchingAttributeAlwaysInline` | ✅ Fixed |
| `MTLFXFrameInterpolatableScaler` | `MTLFXTemporalScalerBase`, `MTLFXTemporalDenoisedScalerBase` | ✅ Fixed |

These protocols now exist in `metal-ast.json` and the affected classes properly inherit from them instead of `NSObject`.

## 4. Deprecated Bool8 Property Aliases (23 properties)

The old generator (via `CppParser`) generated *two* properties for each boolean property whose ObjC getter had an `is` prefix:

```
// Old output (master branch)
public Bool8 IsRasterizationEnabled { get; set; }   // from getter: isRasterizationEnabled

[Obsolete("please use isRasterizationEnabled instead")]
public Bool8 RasterizationEnabled { get; set; }      // deprecated alias (no "is" prefix)
```

The new generator emits only the `IsRasterizationEnabled` form, matching the ObjC getter selector. The deprecated aliases are intentionally not generated.

**Full list of removed deprecated aliases:**

| Class | Removed Alias | Replacement (present) |
|---|---|---|
| `MTLAttribute` | `Active` | `IsActive` |
| `MTLAttribute` | `PatchControlPointData` | `IsPatchControlPointData` |
| `MTLAttribute` | `PatchData` | `IsPatchData` |
| `MTLArgument` | `Active` | `IsActive` |
| `MTLBinding` | `Argument` | `IsArgument` |
| `MTLBinding` | `Used` | `IsUsed` |
| `MTLDepthStencilDescriptor` | `DepthWriteEnabled` | `IsDepthWriteEnabled` |
| `MTLMeshRenderPipelineDescriptor` | `AlphaToCoverageEnabled` | `IsAlphaToCoverageEnabled` |
| `MTLMeshRenderPipelineDescriptor` | `AlphaToOneEnabled` | `IsAlphaToOneEnabled` |
| `MTLMeshRenderPipelineDescriptor` | `RasterizationEnabled` | `IsRasterizationEnabled` |
| `MTLRenderPipelineColorAttachmentDescriptor` | `BlendingEnabled` | `IsBlendingEnabled` |
| `MTLRenderPipelineDescriptor` | `AlphaToCoverageEnabled` | `IsAlphaToCoverageEnabled` |
| `MTLRenderPipelineDescriptor` | `AlphaToOneEnabled` | `IsAlphaToOneEnabled` |
| `MTLRenderPipelineDescriptor` | `RasterizationEnabled` | `IsRasterizationEnabled` |
| `MTLRenderPipelineDescriptor` | `TessellationFactorScaleEnabled` | `IsTessellationFactorScaleEnabled` |
| `MTLTexture` | `FramebufferOnly` | `IsFramebufferOnly` |
| `MTLTexture` | `Shareable` | `IsShareable` |
| `MTLTextureBinding` | `DepthTexture` | `IsDepthTexture` |
| `MTLVertexAttribute` | `Active` | `IsActive` |
| `MTLVertexAttribute` | `PatchControlPointData` | `IsPatchControlPointData` |
| `MTLVertexAttribute` | `PatchData` | `IsPatchData` |
| `MTL4MeshRenderPipelineDescriptor` | `RasterizationEnabled` | `IsRasterizationEnabled` |
| `MTL4RenderPipelineDescriptor` | `RasterizationEnabled` | `IsRasterizationEnabled` |

## 5. Properties Absent from `metal-ast.json` (4 properties)

These properties exist in the old `metal-cpp` headers but are not present in `metal-ast.json`. They may be defined in the missing base protocol `MTLFXFrameInterpolatableScaler` or in a different SDK version.

| Class | Property | Type |
|---|---|---|
| `MTLFXTemporalScalerBase` | `ReactiveTextureFormat` | `MTLPixelFormat` |
| `MTLFXTemporalDenoisedScalerDescriptor` | `IsInputContentPropertiesEnabled` | `Bool8` |
| `MTLFXTemporalDenoisedScalerDescriptor` | `InputContentMaxScale` | `float` |
| `MTLFXTemporalDenoisedScalerDescriptor` | `InputContentMinScale` | `float` |

### Potential fix

Update the `metal-ast.json` extraction tool to include the `MTLFXFrameInterpolatableScaler` protocol and all its members.

---

## 6. `NSOperatingSystemVersion` Struct

The `NSOperatingSystemVersion` struct (3 fields: `MajorVersion`, `MinorVersion`, `PatchVersion`) was generated from `metal-cpp` headers but is not in `metal-ast.json`. It is not referenced by any generated class in the project.

---

*Generated on 2026-03-17.*
