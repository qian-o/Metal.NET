name: Extract Metal API

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  extract:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v5

      - name: Print environment
        run: |
          xcodebuild -version
          echo "SDK: $(xcrun --show-sdk-path)"
          echo "SDK version: $(xcrun --show-sdk-version)"

      - name: Generate API snapshot
        run: |
          SDK_PATH=$(xcrun --show-sdk-path)
          SDK_VERSION=$(xcrun --show-sdk-version)
          DEPLOY_TARGET="macos$(echo $SDK_VERSION | cut -d. -f1).0"
          TEMP_HEADER=$(mktemp /tmp/metal_XXXXXX.h)
          RAW_AST=$(mktemp /tmp/metal_ast_XXXXXX.json)

          cat > "$TEMP_HEADER" <<'HEADER'
          #import <Metal/Metal.h>
          #import <Foundation/Foundation.h>
          #import <QuartzCore/QuartzCore.h>
          #if __has_include(<MetalFX/MetalFX.h>)
          #import <MetalFX/MetalFX.h>
          #endif
          HEADER

          echo "Clang AST dump (target: $DEPLOY_TARGET)..."
          xcrun clang \
            -Xclang -ast-dump=json \
            -fsyntax-only \
            -x objective-c \
            -isysroot "$SDK_PATH" \
            -target "arm64-apple-${DEPLOY_TARGET}" \
            -std=gnu17 \
            -Wno-everything \
            "$TEMP_HEADER" \
            2>/dev/null \
            > "$RAW_AST"
          rm -f "$TEMP_HEADER"
          echo "Raw AST: $(du -h "$RAW_AST" | cut -f1)"

          echo "Extracting Metal API..."
          python3 - "$RAW_AST" "Metal.NET.Generator/metal-ast.json" "$SDK_VERSION" "$SDK_PATH" <<'PYEOF'
          import json, sys, os, re

          raw_path, out_path, sdk_version, sdk_path = sys.argv[1:5]

          WANTED_PREFIXES = ("MTL", "MTLFX")
          WANTED_EXTRA = {
              "NSObject", "NSString", "NSError", "NSArray", "NSDictionary",
              "NSURL", "NSBundle", "NSData", "NSNumber",
              "NSRange", "NSNotificationName",
              "CAMetalLayer", "CAMetalDrawable",
              "CGSize", "CGColorSpaceRef",
          }

          def is_wanted(name):
              if not name:
                  return False
              return any(name.startswith(p) for p in WANTED_PREFIXES) or name in WANTED_EXTRA

          def get_attrs(node):
              """Extract deprecation info from a node."""
              deprecated = False
              dep_message = None
              children = node.get("inner", []) + node.get("attrs", [])
              for child in children:
                  kind = child.get("kind", "")
                  if kind == "AvailabilityAttr":
                      if child.get("deprecated"):
                          deprecated = True
                          if child.get("message"):
                              dep_message = child["message"]
                          elif child.get("replacement"):
                              r = child["replacement"]
                              dep_message = r if r.lower().startswith("use ") else f"Use {r} instead"
                  elif kind == "DeprecatedAttr":
                      deprecated = True
                      if child.get("message"):
                          dep_message = child["message"]
              result = {}
              if deprecated:
                  result["deprecated"] = True
                  if dep_message:
                      result["deprecationMessage"] = dep_message
              return result

          def _apply_deprecation(target, attrs):
              if attrs.get("deprecated"):
                  target["deprecated"] = True
                  if attrs.get("deprecationMessage"):
                      target["deprecationMessage"] = attrs["deprecationMessage"]

          def get_nullability(type_str):
              if not type_str:
                  return None
              if "_Nullable" in type_str:
                  return "nullable"
              if "_Nonnull" in type_str:
                  return "nonnull"
              return None

          def clean_type(type_str):
              if not type_str:
                  return type_str
              type_str = re.sub(r'\s*API_AVAILABLE\((?:[^()]*\([^()]*\))*[^()]*\)', '', type_str)
              type_str = re.sub(r'\bAPI_AVAILABLE\b\s*', '', type_str)
              type_str = re.sub(r'\bNS_RETURNS_RETAINED\b\s*', '', type_str)
              return type_str.strip()

          # NOTE: framework is now tracked via last_file in the main loop
          # because clang AST only emits loc.file on file transitions.

          def extract_fields(record_node):
              fields = []
              for c in record_node.get("inner", []):
                  if c.get("kind") == "FieldDecl":
                      fields.append({
                          "name": c.get("name", ""),
                          "type": clean_type((c.get("type") or {}).get("qualType", "")),
                      })
              return fields

          def find_struct_fields(node):
              if node.get("kind") == "RecordDecl":
                  fields = extract_fields(node)
                  if fields:
                      return fields
              for c in node.get("inner", []):
                  result = find_struct_fields(c)
                  if result:
                      return result
              return None

          def collect_struct_defs(node, by_name, by_id, depth=0):
              if node.get("kind") == "RecordDecl" and node.get("tagUsed") == "struct":
                  fields = extract_fields(node)
                  if fields:
                      rec_id = node.get("id", "")
                      rec_name = node.get("name", "")
                      if rec_id:
                          by_id[rec_id] = fields
                      if rec_name:
                          by_name[rec_name] = fields
              if depth < 5:
                  for c in node.get("inner", []):
                      collect_struct_defs(c, by_name, by_id, depth + 1)

          def find_owned_tag_id(node):
              otd = node.get("ownedTagDecl")
              if otd:
                  return otd.get("id")
              for c in node.get("inner", []):
                  r = find_owned_tag_id(c)
                  if r:
                      return r
              return None

          def extract_enum(node):
              name = node.get("name", "")
              if not name:
                  return None
              members = []
              next_value = 0
              for c in node.get("inner", []):
                  if c.get("kind") == "EnumConstantDecl":
                      m = {"name": c["name"]}
                      found_value = None
                      for v in c.get("inner", []):
                          if v.get("value"):
                              found_value = v["value"]
                              break
                          for v2 in v.get("inner", []):
                              if v2.get("value"):
                                  found_value = v2["value"]
                                  break
                      if found_value is not None:
                          m["value"] = found_value
                          try:
                              next_value = int(found_value, 0) + 1
                          except (ValueError, TypeError):
                              next_value += 1
                      else:
                          m["value"] = str(next_value)
                          next_value += 1
                      _apply_deprecation(m, get_attrs(c))
                      members.append(m)
              if not members:
                  return None
              r = {
                  "kind": "enum",
                  "name": name,
                  "underlyingType": (node.get("fixedUnderlyingType") or {}).get("qualType"),
                  "isOptions": any(
                      c.get("kind") == "FlagEnumAttr"
                      for c in node.get("inner", []) + node.get("attrs", [])
                  ),
                  "members": members,
              }
              _apply_deprecation(r, get_attrs(node))
              return r

          def extract_method(node):
              name = node.get("name", "")
              if not name:
                  return None
              ret = clean_type((node.get("returnType") or {}).get("qualType", "void"))
              params = []
              for c in node.get("inner", []):
                  if c.get("kind") == "ParmVarDecl":
                      pt = clean_type((c.get("type") or {}).get("qualType", ""))
                      params.append({
                          "name": c.get("name", ""),
                          "type": pt,
                          "nullability": get_nullability(pt),
                      })
              r = {
                  "selector": name,
                  "isClassMethod": not node.get("instance", True),
                  "returnType": ret,
                  "returnNullability": get_nullability(ret),
                  "parameters": params,
              }
              _apply_deprecation(r, get_attrs(node))
              return r

          def extract_property(node):
              name = node.get("name", "")
              if not name:
                  return None
              pt = clean_type((node.get("type") or {}).get("qualType", ""))
              r = {
                  "name": name,
                  "type": pt,
                  "nullability": get_nullability(pt),
                  "readonly": node.get("readonly", False),
              }
              if node.get("getter"):
                  r["getter"] = node["getter"].get("name", "")
              if node.get("setter"):
                  r["setter"] = node["setter"].get("name", "")
              _apply_deprecation(r, get_attrs(node))
              return r

          def extract_type(node):
              name = node.get("name", "")
              kind_str = node.get("kind", "")
              r = {
                  "kind": "protocol" if "Protocol" in kind_str else "class",
                  "name": name,
                  "methods": [],
                  "properties": [],
                  "protocols": [p.get("name", "") for p in (node.get("protocols") or [])],
              }
              if node.get("super"):
                  r["super"] = node["super"].get("name", "")
              for c in node.get("inner", []):
                  ck = c.get("kind", "")
                  if ck == "ObjCMethodDecl":
                      m = extract_method(c)
                      if m:
                          r["methods"].append(m)
                  elif ck == "ObjCPropertyDecl":
                      p = extract_property(c)
                      if p:
                          r["properties"].append(p)
              _apply_deprecation(r, get_attrs(node))
              return r

          def _skip_balanced_parens(text, start):
              depth, i = 0, start
              while i < len(text):
                  if text[i] == '(':
                      depth += 1
                  elif text[i] == ')':
                      depth -= 1
                      if depth == 0:
                          return i + 1
                  i += 1
              return i

          def _strip_api_macros(text):
              out, i = [], 0
              while i < len(text):
                  if any(text[i:].startswith(p) for p in (
                      'API_DEPRECATED', 'API_AVAILABLE', 'NS_AVAILABLE',
                      'NS_DEPRECATED', 'NS_RETURNS_RETAINED',
                  )):
                      while i < len(text) and text[i] != '(':
                          i += 1
                      if i < len(text):
                          i = _skip_balanced_parens(text, i)
                  else:
                      out.append(text[i])
                      i += 1
              return ''.join(out)

          def _extract_objc_selector(decl):
              clean = _strip_api_macros(decl).strip().rstrip(';').strip()
              if not clean:
                  return None
              clean = re.sub(r'^[-+]\s*', '', clean)
              if clean and clean[0] == '(':
                  clean = clean[_skip_balanced_parens(clean, 0):].strip()
              no_parens, d = [], 0
              for c in clean:
                  if c == '(':
                      d += 1
                  elif c == ')':
                      d -= 1
                  elif d == 0:
                      no_parens.append(c)
              no_parens = ''.join(no_parens)
              parts = re.findall(r'(\w+)\s*:', no_parens)
              if parts:
                  return ':'.join(parts) + ':'
              m = re.match(r'(\w+)', no_parens.strip())
              return m.group(1) if m else None

          def scan_header_deprecations(sdk_path):
              dep = {}
              for fw in ("Metal", "MetalFX"):
                  scan_dir = os.path.join(sdk_path, f"System/Library/Frameworks/{fw}.framework/Headers")
                  if not os.path.isdir(scan_dir):
                      continue
                  for fn in sorted(os.listdir(scan_dir)):
                      if not fn.endswith('.h'):
                          continue
                      with open(os.path.join(scan_dir, fn)) as hf:
                          text = hf.read()
                      text = re.sub(r'/\*.*?\*/', ' ', text, flags=re.DOTALL)
                      text = re.sub(r'//[^\n]*', '', text)
                      lines = text.split('\n')
                      merged, buf = [], ''
                      for raw in lines:
                          s = raw.strip()
                          if not s:
                              if buf:
                                  merged.append(buf)
                                  buf = ''
                              continue
                          if re.match(r'^@(protocol|interface|end|class|optional|required)\b', s) or s.startswith('#'):
                              if buf:
                                  if 'API_DEPRECATED' in buf and re.match(r'^@(protocol|interface)\b', s):
                                      s = buf + ' ' + s
                                  else:
                                      merged.append(buf)
                                  buf = ''
                              merged.append(s)
                              continue
                          buf = (buf + ' ' + s) if buf else s
                          if s.endswith(';') or s.endswith('{') or s.endswith('}'):
                              merged.append(buf.strip())
                              buf = ''
                      if buf:
                          merged.append(buf.strip())
                      current_type = None
                      for mline in merged:
                          tm = re.search(r'@protocol\s+(\w+)', mline)
                          if tm:
                              current_type = tm.group(1)
                              if 'API_DEPRECATED' in mline:
                                  msg = _extract_dep_message(mline)
                                  if msg:
                                      dep[(current_type, 'type', current_type)] = msg
                              continue
                          tm = re.search(r'@interface\s+(\w+)', mline)
                          if tm:
                              current_type = tm.group(1)
                              is_category = bool(re.search(r'@interface\s+\w+\s*\(', mline))
                              if 'API_DEPRECATED' in mline and not is_category:
                                  msg = _extract_dep_message(mline)
                                  if msg:
                                      dep[(current_type, 'type', current_type)] = msg
                              continue
                          if mline.startswith('@end'):
                              current_type = None
                              continue
                          if not current_type or 'API_DEPRECATED' not in mline:
                              continue
                          msg = _extract_dep_message(mline)
                          if not msg:
                              continue
                          if '@property' in mline:
                              pm = re.search(r'\b(\w+)\s+API_DEPRECATED', mline)
                              if pm:
                                  dep[(current_type, 'property', pm.group(1))] = msg
                          elif re.match(r'\s*[-+]', mline):
                              sel = _extract_objc_selector(mline)
                              if sel:
                                  dep[(current_type, 'method', sel)] = msg
              return dep

          def apply_header_deprecations(api, dep):
              applied = 0
              for collection in (api['protocols'], api['classes']):
                  for t in collection:
                      tname = t['name']
                      type_key = (tname, 'type', tname)
                      if type_key in dep and not t.get('deprecated'):
                          t['deprecated'] = True
                          t['deprecationMessage'] = dep[type_key]
                          applied += 1
                      for p in t.get('properties', []):
                          key = (tname, 'property', p['name'])
                          if key in dep and not p.get('deprecated'):
                              p['deprecated'] = True
                              p['deprecationMessage'] = dep[key]
                              applied += 1
                      for m in t.get('methods', []):
                          key = (tname, 'method', m['selector'])
                          if key in dep and not m.get('deprecated'):
                              m['deprecated'] = True
                              m['deprecationMessage'] = dep[key]
                              applied += 1
              return applied

          def _split_enum_members(body):
              """Split enum body by commas, respecting balanced parentheses."""
              members = []
              current = []
              depth = 0
              for ch in body:
                  if ch == '(':
                      depth += 1
                      current.append(ch)
                  elif ch == ')':
                      depth -= 1
                      current.append(ch)
                  elif ch == ',' and depth == 0:
                      members.append(''.join(current).strip())
                      current = []
                  else:
                      current.append(ch)
              last = ''.join(current).strip()
              if last:
                  members.append(last)
              return members

          def _extract_dep_message(text):
              """Extract deprecation message from a string containing API_DEPRECATED macros."""
              dm = re.search(r'API_DEPRECATED_WITH_REPLACEMENT\s*\(\s*"([^"]*)"', text)
              if dm:
                  r = dm.group(1)
                  return r if r.lower().startswith('use ') else f"Use {r} instead"
              dm = re.search(r'API_DEPRECATED\s*\(\s*"([^"]*)"', text)
              if dm:
                  return dm.group(1)
              return None

          def scan_enum_header_deprecations(sdk_path):
              """Scan Metal headers for deprecated enum constants and enum-level deprecation."""
              member_dep = {}
              enum_dep = {}
              for fw in ("Metal", "MetalFX"):
                  scan_dir = os.path.join(sdk_path, f"System/Library/Frameworks/{fw}.framework/Headers")
                  if not os.path.isdir(scan_dir):
                      continue
                  for fn in sorted(os.listdir(scan_dir)):
                      if not fn.endswith('.h'):
                          continue
                      with open(os.path.join(scan_dir, fn)) as hf:
                          text = hf.read()
                      text = re.sub(r'/\*.*?\*/', ' ', text, flags=re.DOTALL)
                      text = re.sub(r'//[^\n]*', '', text)
                      for em in re.finditer(r'(?:NS_ENUM|NS_OPTIONS)\s*\([^,]+,\s*(\w+)\)', text):
                          enum_name = em.group(1)
                          brace_start = text.find('{', em.end())
                          if brace_start < 0:
                              continue
                          depth, i = 1, brace_start + 1
                          while i < len(text) and depth > 0:
                              if text[i] == '{':
                                  depth += 1
                              elif text[i] == '}':
                                  depth -= 1
                              i += 1
                          enum_body = text[brace_start + 1:i - 1]
                          for entry in _split_enum_members(enum_body):
                              if 'API_DEPRECATED' not in entry:
                                  continue
                              nm = re.match(r'(\w+)\s', entry)
                              if not nm:
                                  continue
                              msg = _extract_dep_message(entry)
                              if msg:
                                  member_dep[(enum_name, nm.group(1))] = msg
                          # Check for enum-level API_DEPRECATED after closing }
                          semi = text.find(';', i - 1)
                          if semi >= 0:
                              trailing = text[i - 1:semi + 1]
                              if 'API_DEPRECATED' in trailing:
                                  msg = _extract_dep_message(trailing)
                                  if msg:
                                      enum_dep[enum_name] = msg
              return member_dep, enum_dep

          def apply_enum_header_deprecations(api, member_dep, enum_dep):
              applied = 0
              for e in api['enums']:
                  ename = e['name']
                  if ename in enum_dep and not e.get('deprecated'):
                      e['deprecated'] = True
                      e['deprecationMessage'] = enum_dep[ename]
                      applied += 1
                  for m in e.get('members', []):
                      key = (ename, m['name'])
                      if key in member_dep and not m.get('deprecated'):
                          m['deprecated'] = True
                          m['deprecationMessage'] = member_dep[key]
                          applied += 1
              return applied

          def _dedup_by_key(items, key):
              seen = {}
              for item in items:
                  seen[item[key]] = item
              return list(seen.values())

          def _merge_types(items):
              merged = {}
              for item in items:
                  name = item["name"]
                  if name not in merged:
                      merged[name] = item
                  else:
                      existing = merged[name]
                      if item.get("kind") != "category" and item.get("framework"):
                          existing["framework"] = item["framework"]
                      seen_sel = {m["selector"] for m in existing["methods"]}
                      for m in item["methods"]:
                          if m["selector"] not in seen_sel:
                              existing["methods"].append(m)
                              seen_sel.add(m["selector"])
                      seen_prop = {p["name"] for p in existing["properties"]}
                      for p in item["properties"]:
                          if p["name"] not in seen_prop:
                              existing["properties"].append(p)
                              seen_prop.add(p["name"])
                      seen_proto = set(existing.get("protocols", []))
                      for p in item.get("protocols", []):
                          if p not in seen_proto:
                              existing["protocols"].append(p)
                              seen_proto.add(p)
                      if not existing.get("super") and item.get("super"):
                          existing["super"] = item["super"]
                      if not existing.get("deprecated") and item.get("deprecated") and item.get("kind") != "category":
                          existing["deprecated"] = True
                          if item.get("deprecationMessage"):
                              existing["deprecationMessage"] = item["deprecationMessage"]
              return list(merged.values())

          # === Main ===
          with open(raw_path) as f:
              ast = json.load(f)

          attr_kind_counts = {}
          stack = [(n, False) for n in ast.get("inner", [])]
          while stack:
              node, in_objc = stack.pop()
              k = node.get("kind", "")
              is_objc = in_objc or k.startswith("ObjC")
              if is_objc and "Attr" in k:
                  attr_kind_counts[k] = attr_kind_counts.get(k, 0) + 1
              for key in ("inner", "attrs"):
                  for c in node.get(key, []):
                      stack.append((c, is_objc))
          if attr_kind_counts:
              print(f"ObjC attribute kinds: {dict(sorted(attr_kind_counts.items()))}")
          else:
              print("WARNING: No attribute kinds found in ObjC declarations")

          struct_by_name, struct_by_id = {}, {}
          for node in ast.get("inner", []):
              collect_struct_defs(node, struct_by_name, struct_by_id)
          print(f"Pre-scan: {len(struct_by_name)} named + {len(struct_by_id)} by-id struct definitions")

          # Pre-scan TypedefDecl nodes for deprecation info.
          # API_DEPRECATED on NS_ENUM / NS_OPTIONS attaches to the TypedefDecl,
          # not the inner EnumDecl, so we need to forward it.
          typedef_deprecations = {}
          for node in ast.get("inner", []):
              if node.get("kind") == "TypedefDecl":
                  td_name = node.get("name", "")
                  if td_name and is_wanted(td_name):
                      td_attrs = get_attrs(node)
                      if td_attrs.get("deprecated"):
                          typedef_deprecations[td_name] = td_attrs
          if typedef_deprecations:
              print(f"Pre-scan: {len(typedef_deprecations)} typedef deprecation(s) found")

          api = {
              "sdkVersion": sdk_version,
              "enums": [], "protocols": [], "classes": [],
              "typedefs": [], "functions": [], "structs": [],
          }

          last_file = ""
          for node in ast.get("inner", []):
              loc = node.get("loc", {})
              for _key in ("file", ):
                  _fp = loc.get(_key, "")
                  if _fp:
                      last_file = _fp
                      break
              else:
                  for _sub in ("spellingLoc", "expansionLoc"):
                      _fp = loc.get(_sub, {}).get("file", "")
                      if _fp:
                          last_file = _fp
                          break
              _fw_match = re.search(r'/(\w+)\.framework/', last_file)
              fw = _fw_match.group(1) if _fw_match else None

              kind = node.get("kind", "")
              name = node.get("name", "")
              if kind == "ObjCCategoryDecl":
                  cat_name = node.get("interface", {}).get("name", "")
                  if is_wanted(cat_name):
                      t = extract_type(node)
                      t["name"] = cat_name
                      t["kind"] = "category"
                      t["framework"] = fw
                      if t["methods"] or t["properties"]:
                          api["classes"].append(t)
                  continue
              if not is_wanted(name):
                  continue
              if kind == "EnumDecl":
                  e = extract_enum(node)
                  if e:
                      e["framework"] = fw
                      if not e.get("deprecated") and e["name"] in typedef_deprecations:
                          _apply_deprecation(e, typedef_deprecations[e["name"]])
                      api["enums"].append(e)
              elif kind == "ObjCProtocolDecl":
                  t = extract_type(node)
                  if t:
                      t["framework"] = fw
                      api["protocols"].append(t)
              elif kind == "ObjCInterfaceDecl":
                  t = extract_type(node)
                  if t:
                      t["framework"] = fw
                      api["classes"].append(t)
              elif kind == "TypedefDecl":
                  ut = (node.get("type") or {}).get("qualType", "")
                  td_entry = {"name": name, "underlyingType": ut, "framework": fw}
                  _apply_deprecation(td_entry, get_attrs(node))
                  api["typedefs"].append(td_entry)
                  if ut.startswith("struct "):
                      fields, method = None, None
                      fields = find_struct_fields(node)
                      if fields:
                          method = "embedded"
                      if not fields:
                          tag_id = find_owned_tag_id(node)
                          if tag_id and tag_id in struct_by_id:
                              fields = struct_by_id[tag_id]
                              method = f"ownedTag({tag_id[:8]})"
                      if not fields:
                          m = re.match(r'^struct\s+(\w+)$', ut)
                          if m:
                              fields = struct_by_name.get(m.group(1))
                              if fields:
                                  method = f"by-name({m.group(1)})"
                      if fields:
                          st_entry = {"name": name, "fields": fields, "framework": fw}
                          _apply_deprecation(st_entry, get_attrs(node))
                          api["structs"].append(st_entry)
                          print(f"  Struct '{name}': {len(fields)} fields via {method}")
                      else:
                          print(f"  WARNING: typedef '{name}' -> '{ut}' could not find struct fields")
              elif kind == "FunctionDecl":
                  ft = (node.get("type") or {}).get("qualType", "")
                  params = []
                  for c in node.get("inner", []):
                      if c.get("kind") == "ParmVarDecl":
                          params.append({
                              "name": c.get("name", ""),
                              "type": clean_type((c.get("type") or {}).get("qualType", "")),
                          })
                  ret_type = ft.split("(")[0].strip() if "(" in ft else ft
                  fn_entry = {
                      "name": name,
                      "returnType": clean_type(ret_type),
                      "parameters": params,
                      "framework": fw,
                  }
                  _apply_deprecation(fn_entry, get_attrs(node))
                  api["functions"].append(fn_entry)
              elif kind == "RecordDecl" and node.get("tagUsed") == "struct":
                  fields = extract_fields(node) or find_struct_fields(node)
                  if fields:
                      st_entry = {"name": name, "fields": fields, "framework": fw}
                      _apply_deprecation(st_entry, get_attrs(node))
                      api["structs"].append(st_entry)
              elif kind == "RecordDecl":
                  tag = node.get("tagUsed", "<none>")
                  if tag != "union":
                      fields = find_struct_fields(node)
                      if fields:
                          st_entry = {"name": name, "fields": fields, "framework": fw}
                          _apply_deprecation(st_entry, get_attrs(node))
                          api["structs"].append(st_entry)
                          print(f"  Struct '{name}' via RecordDecl (tagUsed={tag}): {len(fields)} fields")

          api["enums"] = _dedup_by_key(api["enums"], "name")
          api["functions"] = _dedup_by_key(api["functions"], "name")
          api["structs"] = _dedup_by_key(api["structs"], "name")
          api["classes"] = _merge_types(api["classes"])
          api["protocols"] = _merge_types(api["protocols"])

          dep_from_headers = scan_header_deprecations(sdk_path)
          applied = apply_header_deprecations(api, dep_from_headers)
          print(f"Header scan: {len(dep_from_headers)} deprecation annotations found, {applied} applied")

          enum_member_dep, enum_type_dep = scan_enum_header_deprecations(sdk_path)
          enum_applied = apply_enum_header_deprecations(api, enum_member_dep, enum_type_dep)
          print(f"Enum header scan: {len(enum_member_dep)} member + {len(enum_type_dep)} enum-level deprecation(s), {enum_applied} applied")

          print(f"Enums: {len(api['enums'])}, Protocols: {len(api['protocols'])}, "
                f"Classes: {len(api['classes'])}, Functions: {len(api['functions'])}, "
                f"Structs: {len(api['structs'])}, Typedefs: {len(api['typedefs'])}")
          dep_enums = sum(1 for e in api['enums'] if e.get('deprecated'))
          dep_enum_members = sum(1 for e in api['enums']
                                for m in e.get('members', []) if m.get('deprecated'))
          dep_types = sum(1 for t in api['protocols'] + api['classes'] if t.get('deprecated'))
          dep_methods = sum(1 for t in api['protocols'] + api['classes']
                           for m in t.get('methods', []) if m.get('deprecated'))
          dep_props = sum(1 for t in api['protocols'] + api['classes']
                         for p in t.get('properties', []) if p.get('deprecated'))
          dep_funcs = sum(1 for f in api['functions'] if f.get('deprecated'))
          dep_structs = sum(1 for s in api['structs'] if s.get('deprecated'))
          print(f"Deprecated: {dep_enums} enums, {dep_enum_members} enum members, "
                f"{dep_types} types, {dep_methods} methods, {dep_props} properties, "
                f"{dep_funcs} functions, {dep_structs} structs")

          with open(out_path, "w", encoding="utf-8-sig") as f:
              json.dump(api, f, indent=2, ensure_ascii=False)
          print(f"Written: {out_path} ({os.path.getsize(out_path) / 1024:.0f} KB)")
          os.remove(raw_path)
          PYEOF

      - name: Commit changes
        run: |
          SDK_VERSION=$(xcrun --show-sdk-version)
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Metal.NET.Generator/metal-ast.json
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "Update metal-ast.json (SDK $SDK_VERSION)"
          git push

